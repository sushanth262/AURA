# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors (local settings only; keep shared configs if added later)
.idea/
*.code-workspace
*.swp
*~

# Local env (anywhere in repo)
.env
.env.local
.env.*.local

# Local deploy manifests (generated by deploy scripts)
aura-release-*.json

# Go binaries at repo root (if built outside aura-backend)
*.exe
*.exe~
